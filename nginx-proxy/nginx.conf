user www-data;
worker_processes auto;
pid /run/nginx.pid;

load_module "modules/ngx_stream_module.so";

events {
       worker_connections 1024;
}

stream {
    upstream dns_tcp_servers {
        server 8.8.8.8:53; #  Proxy to standard DNS server here
        }

    server {
       listen 853 ssl;
       listen 8443 ssl;  # Might be able to use this instead of dnss?
       proxy_pass dns_tcp_servers;

       ssl_certificate       /etc/letsencrypt/live/dot.naphos.com/fullchain.pem;
       ssl_certificate_key   /etc/letsencrypt/live/dot.naphos.com/privkey.pem;
       ssl_protocols         TLSv1.2;
       ssl_ciphers           ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
       ssl_session_tickets on;
       ssl_session_timeout   8h;
       ssl_handshake_timeout 10s;
       }
}
